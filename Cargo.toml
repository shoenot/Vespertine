[workspace]
members = [
    "kernel", 
    "lib/mnemosyne-abi", 
    "lib/mnemosyne-common", 
    "lib/mnemosyne-rt", "userland/shell"]

resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
