[workspace]
members = [
    "kernel",
    "lib/vespertine-abi",
    "lib/vespertine-common",
    "lib/vespertine-rt", "userland/hesper", "userland/shell"]

resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
